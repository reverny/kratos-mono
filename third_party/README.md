# Third-party Proto Files

This directory contains third-party proto files required for the project.

The actual proto files will be managed by Buf and downloaded automatically when needed.

For googleapis proto files, we use `buf.build/googleapis/googleapis` as a dependency in `buf.yaml`.
